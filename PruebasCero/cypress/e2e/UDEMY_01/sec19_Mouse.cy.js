///<reference types="Cypress"/>
import 'cypress-file-upload'

describe('Trabajo con el Mouse', () => {
    it('Mouse', () => {
      cy.visit('https://example.cypress.io')
    })
  
 
          Cypress.on('uncaught:exception', (err, runnable) => {
            return false;
           }); 
  
  })